import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class AttendanceDetailPage extends StatelessWidget {
  static String routeName(int id) => '/attendances/$id';

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
